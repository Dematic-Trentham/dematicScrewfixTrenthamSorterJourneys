model siteEMS {
  id          String   @id @default(uuid())
  lastUpdated DateTime @default(now())
  name        String   @unique
  location    String
  subLocation String
  description String
  value       String
}
